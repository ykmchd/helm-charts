apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-vector-chart.fullname" . }}
  labels:
    {{- include "nginx-vector-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.nginx.service.type }}
  ports:
    - port: {{ .Values.nginx.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-vector-chart.selectorLabels" . | nindent 4 }}